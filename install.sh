# This script will install and config as much of the system as possible from the terminal.
# At the end of certain scripts are commented out portions. Uncomment if needed.
# TODO: add options for the user so that uncommenting is not needed

source bin/appimage
source bin/dnf
source bin/flatpak
source bin/git
source bin/other
